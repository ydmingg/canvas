import CanvasType from '../CanvasType'
import { CanvasRender } from '../CanvasRender'

// 导出舞台
export default class image { 
    
    constructor(shapeType:CanvasType, render: CanvasRender) {
        // const image = render.newImage({
        //     id: shapeType.id,
        //     title: shapeType.title,
        //     name: "board",
        //     image: shapeType.style.imageSrc,
        //     x: shapeType.style.x,
        //     y: shapeType.style.y,
        //     width: shapeType.style.width,
        //     height: shapeType.style.height,
        //     scaleX: shapeType.style.scaleX,
        //     scaleY: shapeType.style.scaleY,

        // })
        // render.group.add(image)
        
    }
    
    
}